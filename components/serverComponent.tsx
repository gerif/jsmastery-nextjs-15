import React from "react";
import { useTheme } from "next-themes";

const ServerComponent = () => {
  console.log("This is ServerComponent");

  return (
    <div>
      <h1>Server Component</h1>
    </div>
  );
};

export default ServerComponent;
